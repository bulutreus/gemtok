/** @deprecated Import from `./gameConfig` — kept for existing imports. */
export {
  ROUND_SECONDS,
  LEADERBOARD_ROWS,
  WEAPON_DURATION_MS,
  SPEED_BOOST_MS,
  GIFT_LABELS,
  giftToEffect,
} from "./gameConfig";

# GEMTOK UI mockups

| Dosya | Açıklama |
|--------|-----------|
| `gemtok-unified-game-settings.html` | Piksel referansı — koyu navy, neon mavi, modal + slider + TikTok / hediye bölümleri. |

**Canlı uygulama:** Tüm oyunların **mevcut** ayar panelleri aynı görünüm için `sira/gemtok-settings-theme.css` dosyasını yükler; ayar köküne `gemtok-settings-theme` sınıfı eklenir (ayrı modal veya ek ayar alanı yok).

Tarayıcıda bu klasördeki HTML’i doğrudan açın.
